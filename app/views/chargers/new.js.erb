

//ERROR HANDLING

//remove previous validation errrors

var errorChargerNoDiv = document.getElementById('error-charger-no')

var chargerNoInput = document.getElementById('charger-no-input')

errorChargerNoDiv.innerHTML = "<%= escape_javascript(@errorChargerNo[0]) %>"

<% if @resetForm == "1" %>
  document.getElementById("charger-form").reset()
<% else %>
  chargerNoInput.classList.add('is-invalid')
<% end %>


//Add new input to page via AJAX
var chargerList = document.getElementById('charger-list')
var newCharger = "<%= escape_javascript( render 'chargers/charger', charger: @charger ) %>"

var div = document.createElement('div')

div.innerHTML = newCharger

chargerList.appendChild(div.firstChild)
